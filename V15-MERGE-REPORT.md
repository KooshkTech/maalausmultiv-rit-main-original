# V15 merge report

Base: current domain project supplied as `maalausmultivari-fixed (2).zip`.

## Preserved
- Entire `public/images/` tree from the domain baseline.
- Existing image paths in `src/config/images.ts`.
- Existing domain deployment files and PHP mail endpoint.

## Merged V15 changes
- Standardized workmanship guarantee references from 5 years to 2 years.
- Strengthened Helsinki, Espoo and Vantaa local SEO metadata.
- Added `npm run verify:images` and `npm run verify`.
- Added automated image integrity verification.
- Added root `.gitignore`.

## Verification target
Run `npm install` followed by `npm run verify`.
