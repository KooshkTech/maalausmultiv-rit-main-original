# QA notes

The new V3 page is intentionally activated only on the feature branch. CI and manual browser testing are required before merge. Priority QA: image loading, canvas coordinate mapping, Add/Remove corrections, independent colors, undo/redo, Before/After/Compare, JPG/PNG, portrait mobile viewport, and fallback behavior without WebGPU.
