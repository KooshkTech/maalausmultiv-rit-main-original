# Rollback

This work is isolated on `feature/varikamu-v3-semantic-editor`. Production `main` is not changed until review/merge. If V3 fails acceptance, close the PR or revert the activation commit; the previous editor remains available in Git history. Do not deploy an unverified semantic model directly to `main`.
