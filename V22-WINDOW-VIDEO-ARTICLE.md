# V22 Window painting video article

- Replaced the earlier standalone HTML approach with a native React page.
- Added `/blogi/ikkunoiden-maalaus` using the existing site Layout, Header, Footer and SEO component.
- Added three responsive project videos with controls, metadata preload and no autoplay.
- Added Article + VideoObject structured data.
- Added internal links to window/door painting, exterior painting, maintenance painting and Vantaa/Helsinki/Espoo pages.
- Featured the article on the existing blog landing page.
- Restored the existing robots.txt and removed the separate video sitemap/standalone HTML page from this branch.
- Video binaries are deployed separately to `/videos/window-painting/` on HostGator so they are not bundled into the JavaScript app.
